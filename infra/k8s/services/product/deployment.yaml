apiVersion: apps/v1
kind: Deployment
metadata:
  name: product-service
  namespace: shopflow-production
  labels:
    app: product-service
    tier: application
spec:
  replicas: 2
  selector:
    matchLabels:
      app: product-service
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  template:
    metadata:
      labels:
        app: product-service
        tier: application
    spec:
      containers:
        - name: product-service
          image: ghcr.io/raviteja2206/shopflow-productatest
          ports:
            - containerPort: 8002
8003
          envFrom:
            - configMapRef:
                name: shopflow-config
            - secretRef:
                name: shopflow-secrets
          env:
            - name: PORT
              value: "8002
8003"
          readinessProbe:
            httpGet:
              path: /health
              port: 8002
8003
            initialDelaySeconds: 10
            periodSeconds: 5
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /health
              port: 8002
8003
            initialDelaySeconds: 30
            periodSeconds: 10
            failureThreshold: 3
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"
      initContainers:
        - name: wait-for-postgres
          image: busybox:1.36
          command: [sh, -c, "until nc -z postgres-service 5432; do sleep 2; done"]
        - name: wait-for-kafka
          image: busybox:1.36
          command: [sh, -c, "until nc -z kafka-service 9092; do sleep 2; done"]
