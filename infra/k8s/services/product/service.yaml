apiVersion: v1
kind: Service
metadata:
  name: product-service
  namespace: shopflow-production
  labels:
    app: product-service
spec:
  selector:
    app: product-service
  ports:
    - port: 8002
8003
      targetPort: 8002
8003
  type: ClusterIP
