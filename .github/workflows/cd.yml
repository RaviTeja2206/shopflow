name: CD

on:
  workflow_run:
    workflows: [CI]
    types: [completed]
    branches: [main]

jobs:
  deploy-staging:
    name: Deploy to Staging
    runs-on: ubuntu-latest
    # Skip until AWS infrastructure is provisioned (Week 5)
    if: false
    environment: staging

    steps:
      - uses: actions/checkout@v4
      - name: Placeholder
        run: echo "AWS infrastructure not yet provisioned"

  deploy-production:
    name: Deploy to Production
    runs-on: ubuntu-latest
    needs: deploy-staging
    if: false
    environment: production

    steps:
      - uses: actions/checkout@v4
      - name: Placeholder
        run: echo "AWS infrastructure not yet provisioned"
